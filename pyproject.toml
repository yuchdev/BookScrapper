[project]
name = "bookscraper"
version = "1.0.0"
requires-python = ">=3.8"
dynamic = ["description", "readme", "dependencies"]

[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"
